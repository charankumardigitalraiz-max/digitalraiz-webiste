import { LucideIcon } from "lucide-react";

export interface MlCapability {
  title: string;
  desc: string;
  icon: LucideIcon;
}

export interface MlTechStackItem {
  name: string;
  desc: string;
}
